spring:
  application:
    name: wb3-microservice-discovery
  profiles:
    active: default
  cloud:
    config:
      uri: http://config-server:3000
    consul:
      host: service-discovery
      port: 3001
      config:
        enabled: true